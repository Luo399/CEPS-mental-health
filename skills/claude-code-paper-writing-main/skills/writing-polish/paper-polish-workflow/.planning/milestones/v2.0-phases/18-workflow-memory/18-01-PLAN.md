---
phase: 18-workflow-memory
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/config.json
  - .planning/workflow-memory.json
  - references/skill-conventions.md
  - references/skill-skeleton.md
autonomous: true
requirements:
  - UXFIX-03

must_haves:
  truths:
    - "config.json contains a workflow_memory key with threshold, max_history, and max_chain values"
    - "workflow-memory.json exists as an empty JSON array"
    - "skill-conventions.md contains a Workflow Memory section documenting recording rules and pattern detection"
    - "skill-skeleton.md contains Step 0 and record-write step templates"
  artifacts:
    - path: ".planning/config.json"
      provides: "workflow_memory configuration block"
      contains: "workflow_memory"
    - path: ".planning/workflow-memory.json"
      provides: "Empty call history log"
      contains: "[]"
    - path: "references/skill-conventions.md"
      provides: "Workflow Memory convention section"
      contains: "## Workflow Memory"
    - path: "references/skill-skeleton.md"
      provides: "Step 0 and record-write templates"
      contains: "Step 0"
  key_links:
    - from: "references/skill-conventions.md"
      to: ".planning/workflow-memory.json"
      via: "documents the exact write location"
      pattern: "workflow-memory\\.json"
    - from: "references/skill-conventions.md"
      to: ".planning/config.json"
      via: "references config for threshold"
      pattern: "config\\.json.*workflow_memory"
---

<objective>
Create the infrastructure files for the Workflow Memory system: configuration, empty history log, convention rules, and skeleton template updates.

Purpose: Establish the authoritative specification and config before any Skill is modified, so all 12 Skill updates in Plan 02 can reference a single source of truth.
Output: Updated config.json, new workflow-memory.json, updated skill-conventions.md with Workflow Memory section, updated skill-skeleton.md with Step 0 and record-write templates.
</objective>

<execution_context>
@C:/Users/Lylll/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Lylll/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-workflow-memory/18-CONTEXT.md
@.planning/phases/18-workflow-memory/18-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create config and history files</name>
  <files>.planning/config.json, .planning/workflow-memory.json</files>
  <read_first>
    - .planning/config.json (current config — must preserve all existing keys)
    - .planning/phases/18-workflow-memory/18-CONTEXT.md (locked decisions for exact values)
  </read_first>
  <action>
1. Read `.planning/config.json`. It currently contains:
```json
{
  "mode": "yolo",
  "granularity": "fine",
  "parallelization": true,
  "commit_docs": true,
  "model_profile": "quality",
  "workflow": {
    "research": true,
    "plan_check": true,
    "verifier": true,
    "nyquist_validation": true,
    "_auto_chain_active": false
  }
}
```

2. Add a new top-level key `workflow_memory` with these exact values:
```json
"workflow_memory": {
  "threshold": 5,
  "max_history": 50,
  "max_chain": 3
}
```
All existing keys must remain unchanged.

3. Create `.planning/workflow-memory.json` as an empty JSON array:
```json
[]
```
  </action>
  <verify>
    <automated>node -e "const c=require('./.planning/config.json'); const wm=c.workflow_memory; if(!wm||wm.threshold!==5||wm.max_history!==50||wm.max_chain!==3) process.exit(1); console.log('config OK');" && node -e "const h=require('./.planning/workflow-memory.json'); if(!Array.isArray(h)||h.length!==0) process.exit(1); console.log('history OK');"</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/config.json` contains `"workflow_memory"` key
    - `workflow_memory.threshold` equals `5`
    - `workflow_memory.max_history` equals `50`
    - `workflow_memory.max_chain` equals `3`
    - All pre-existing keys (`mode`, `granularity`, `parallelization`, `commit_docs`, `model_profile`, `workflow`) are preserved
    - `.planning/workflow-memory.json` exists and contains exactly `[]`
  </acceptance_criteria>
  <done>config.json has workflow_memory block with correct values; workflow-memory.json is an empty array</done>
</task>

<task type="auto">
  <name>Task 2: Add Workflow Memory section to skill-conventions.md and update skill-skeleton.md</name>
  <files>references/skill-conventions.md, references/skill-skeleton.md</files>
  <read_first>
    - references/skill-conventions.md (current state — find insertion point after existing sections)
    - references/skill-skeleton.md (current state — find Workflow section to add Step 0 and record-write)
    - .planning/phases/18-workflow-memory/18-CONTEXT.md (locked decisions)
    - .planning/phases/18-workflow-memory/18-RESEARCH.md (Example 3, 4, 5 templates)
  </read_first>
  <action>
**Part A: Update skill-conventions.md**

Insert a new `## Workflow Memory` section BEFORE the final `## Maintenance Rules` section (i.e., between `## Ask Strategy Conventions` and `## Naming and Discovery`, or before the `---` separator that precedes `## Naming and Discovery`). The exact content to insert:

```markdown
---

## Workflow Memory

All Skills must participate in the project-wide workflow memory system. This system records Skill invocations and detects frequent sequences to offer workflow recommendations.

### Recording

- After the Skill's first-step validation completes (i.e., when the Skill begins producing output, not on invocation), append one entry to `.planning/workflow-memory.json`.
- Entry format: `{"skill": "<skill-name>", "ts": "<ISO timestamp>"}`
- Maximum 50 entries retained. If log length >= 50, drop the oldest entry before appending.
- If the file does not exist, create it as `[]` before appending.
- If the Skill refuses or exits before completing first-step validation, do NOT write a record.

### Pattern Detection (Step 0)

- At the very start of the Workflow, before any existing Step 1 logic, read `.planning/workflow-memory.json`.
- Check if the last 1-2 entries form a consecutive pattern with the current Skill that appears >= threshold times (configured in `.planning/config.json` under `workflow_memory.threshold`, default: 5).
- Pattern types: 2-item (A->B) or 3-item (A->B->C) consecutive chains. Check 3-item patterns first (more specific wins).
- Count occurrences by scanning the entire log left-to-right: for each position i where log[i..i+N-1] matches the pattern, count +1. Occurrences may share no entries but positions can be non-overlapping.
- If a matching pattern is found, present a bilingual recommendation via AskUserQuestion:
  - Question: "检测到常用流程：[pattern]（已出现 N 次）。是否直接以 direct 模式运行 [current skill]？"
  - Options: "Yes, proceed" (activates direct mode) / "No, continue normally"
- If the user accepts: skip all Ask Strategy questions and run in `direct` mode.
- If the user declines or AskUserQuestion is unavailable: proceed in normal mode.
- Pattern detection must not block the workflow. If the history file is missing, empty, or unparseable, skip silently to Step 1.
```

**Part B: Update skill-skeleton.md**

In the `## Workflow` section, add a new `### Step 0: Workflow Memory Check` subsection BEFORE the existing `### Step 1: Collect Context`. The content to insert:

```markdown
### Step 0: Workflow Memory Check

- Read `.planning/workflow-memory.json`. If file missing or empty, skip to Step 1.
- Check if the last 1-2 entries form a recognized pattern with the current Skill (e.g., polish -> de-ai) that has appeared >= `workflow_memory.threshold` times (default: 5) in the log. See `skill-conventions.md > Workflow Memory > Pattern Detection` for the full algorithm.
- If a pattern is found, present recommendation via AskUserQuestion:
  - Question: "检测到常用流程：[pattern]（已出现 N 次）。是否直接以 direct 模式运行 [current skill]？"
  - Options: "Yes, proceed" / "No, continue normally"
- If user accepts: set mode to `direct` for this invocation, skip Ask Strategy questions.
- If user declines or AskUserQuestion unavailable: continue in normal mode.
```

Also add a record-write note at the end of `### Step 1: Collect Context` (append after the existing last bullet):

```markdown
- **Record workflow:** After validation completes, append `{"skill": "example-skill", "ts": "<ISO timestamp>"}` to `.planning/workflow-memory.json`. If file does not exist, create as `[]` first. If log length >= 50, drop oldest entry before appending.
```
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const c=fs.readFileSync('references/skill-conventions.md','utf8'); const checks=['## Workflow Memory','workflow-memory.json','workflow_memory.threshold','AskUserQuestion','Pattern Detection (Step 0)','50 entries']; const missing=checks.filter(s=>!c.includes(s)); if(missing.length){console.log('MISSING:',missing);process.exit(1)} console.log('conventions OK');" && node -e "const fs=require('fs'); const s=fs.readFileSync('references/skill-skeleton.md','utf8'); const checks=['Step 0: Workflow Memory Check','workflow-memory.json','Record workflow']; const missing=checks.filter(x=>!s.includes(x)); if(missing.length){console.log('MISSING:',missing);process.exit(1)} console.log('skeleton OK');"</automated>
  </verify>
  <acceptance_criteria>
    - `references/skill-conventions.md` contains `## Workflow Memory` section
    - The section contains subsections `### Recording` and `### Pattern Detection (Step 0)`
    - Recording subsection mentions `.planning/workflow-memory.json`, entry format `{"skill":`, 50-entry cap, and "do NOT write a record" refusal rule
    - Pattern Detection subsection mentions `workflow_memory.threshold`, `AskUserQuestion`, 3-item priority over 2-item, bilingual question text with "检测到常用流程", and fallback behavior
    - `references/skill-skeleton.md` contains `### Step 0: Workflow Memory Check` before `### Step 1`
    - Skeleton Step 1 contains `Record workflow:` with append instruction
    - All pre-existing content in both files is preserved
  </acceptance_criteria>
  <done>skill-conventions.md has Workflow Memory section with recording and pattern detection rules; skill-skeleton.md has Step 0 template and record-write in Step 1</done>
</task>

</tasks>

<verification>
1. `node -e "require('./.planning/config.json').workflow_memory.threshold === 5 || process.exit(1)"` exits 0
2. `node -e "JSON.parse(require('fs').readFileSync('.planning/workflow-memory.json','utf8')).length === 0 || process.exit(1)"` exits 0
3. `grep -c '## Workflow Memory' references/skill-conventions.md` returns 1
4. `grep -c 'Step 0: Workflow Memory Check' references/skill-skeleton.md` returns 1
</verification>

<success_criteria>
- config.json has workflow_memory with threshold=5, max_history=50, max_chain=3
- workflow-memory.json exists as empty array
- skill-conventions.md documents the complete Workflow Memory rules (recording + pattern detection)
- skill-skeleton.md includes Step 0 template and record-write instruction in Step 1
</success_criteria>

<output>
After completion, create `.planning/phases/18-workflow-memory/18-01-SUMMARY.md`
</output>
