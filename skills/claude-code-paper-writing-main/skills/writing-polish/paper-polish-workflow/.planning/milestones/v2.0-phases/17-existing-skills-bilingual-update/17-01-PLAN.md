---
phase: 17-existing-skills-bilingual-update
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .claude/skills/translation-skill/SKILL.md
  - .claude/skills/reviewer-simulation-skill/SKILL.md
  - .claude/skills/logic-skill/SKILL.md
  - .claude/skills/polish-skill/SKILL.md
  - .claude/skills/de-ai-skill/SKILL.md
  - .claude/skills/abstract-skill/SKILL.md
  - .claude/skills/experiment-skill/SKILL.md
autonomous: true
requirements:
  - BILN-04

must_haves:
  truths:
    - "All 7 Skills include references/bilingual-output.md in their references frontmatter"
    - "All 7 Skills detect opt-out keywords (english only, no bilingual, only english, 不要中文) and skip Chinese when detected"
    - "translation-skill and reviewer-simulation-skill: bilingual output behavior unchanged, opt-out now honored"
    - "logic-skill: all **[中文]** labels replaced with **[Chinese]** in report format and notes"
    - "polish-skill and de-ai-skill: after in-place file edits, each modified paragraph has a > **[Chinese]** blockquote displayed in conversation"
    - "abstract-skill: each output sentence (labeled and clean versions) has a > **[Chinese]** blockquote in conversation"
    - "experiment-skill: each Phase 2 discussion paragraph has a > **[Chinese]** blockquote appended in conversation"
  artifacts:
    - path: ".claude/skills/translation-skill/SKILL.md"
      provides: "Opt-out detection + bilingual-output.md reference"
      contains: "references/bilingual-output.md"
    - path: ".claude/skills/reviewer-simulation-skill/SKILL.md"
      provides: "Opt-out detection + bilingual-output.md reference"
      contains: "references/bilingual-output.md"
    - path: ".claude/skills/logic-skill/SKILL.md"
      provides: "Label fix + opt-out + reference"
      contains: "**[Chinese]**"
    - path: ".claude/skills/polish-skill/SKILL.md"
      provides: "Bilingual conversation output + opt-out + reference"
      contains: "bilingual_conversation"
    - path: ".claude/skills/de-ai-skill/SKILL.md"
      provides: "Bilingual conversation output + opt-out + reference"
      contains: "bilingual_conversation"
    - path: ".claude/skills/abstract-skill/SKILL.md"
      provides: "Bilingual blockquote per sentence + opt-out + reference"
      contains: "bilingual_abstract"
    - path: ".claude/skills/experiment-skill/SKILL.md"
      provides: "Bilingual blockquote per paragraph + opt-out + reference"
      contains: "bilingual_discussion"
  key_links:
    - from: "each SKILL.md"
      to: "references/bilingual-output.md"
      via: "references.required frontmatter list"
      pattern: "references/bilingual-output.md"
    - from: "Workflow opt-out step"
      to: "bilingual output block"
      via: "keyword check gates all Chinese output"
      pattern: "english only|no bilingual|only english|不要中文"
---

<objective>
Update 7 existing Skills to support bilingual (English + Chinese paragraph-by-paragraph) output as the default mode, with opt-out keyword detection.

Purpose: BILN-04 compliance — all eligible Skills produce bilingual output by default so users reading or reviewing their own academic text receive Chinese comprehension aid inline. Users who want English-only output use one of four opt-out phrases.

Output: 7 updated SKILL.md files, each with the bilingual-output.md reference, opt-out detection, and the appropriate bilingual output variant per their output type.
</objective>

<execution_context>
@C:/Users/Lylll/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Lylll/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@references/bilingual-output.md
@references/skill-conventions.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: translation-skill — add opt-out + reference</name>
  <files>.claude/skills/translation-skill/SKILL.md</files>
  <action>
Read the current SKILL.md first.

**Change 1 — frontmatter `references.required`** (currently lines 20-22):
Add `references/bilingual-output.md` as the second item in the `required` list, after `references/expression-patterns.md`:

```yaml
references:
  required:
    - references/expression-patterns.md
    - references/bilingual-output.md
```

**Change 2 — Workflow Step 3 (Generate Output Files)**, add opt-out check at the start of the step:

Insert this block immediately before the "Write English-only version to `[name]_en.tex`" sentence (currently the first sentence of Step 3):

```
- **Opt-out check:** Before generating output, scan the user's original trigger prompt for any of these phrases (case-insensitive, exact phrase match): `english only`, `no bilingual`, `only english`, `不要中文`. If any phrase is detected: skip the bilingual file entirely — produce only `[name]_en.tex`. If none detected: produce both files as normal.
```

No other changes. The bilingual_tex output contract entry already says "Always produced" — update that row's Condition to: `Always produced (unless opt-out keyword detected)`.

In the Output Contract table, change the `bilingual_tex` row:

| `bilingual_tex` | LaTeX file (`[name]_bilingual.tex`) | Always produced unless opt-out keyword detected |

**Line budget check:** Current file is 211 lines. These additions add ~6 lines. Expected total ~217 lines — within 300-line budget.
  </action>
  <verify>
    <automated>grep -n "bilingual-output.md" ".claude/skills/translation-skill/SKILL.md" && grep -n "Opt-out check" ".claude/skills/translation-skill/SKILL.md" && grep -n "opt-out keyword detected" ".claude/skills/translation-skill/SKILL.md"</automated>
  </verify>
  <done>SKILL.md contains references/bilingual-output.md in references.required, an opt-out check step in Workflow Step 3, and updated Output Contract condition for bilingual_tex.</done>
</task>

<task type="auto">
  <name>Task 2: reviewer-simulation-skill — add opt-out + reference</name>
  <files>.claude/skills/reviewer-simulation-skill/SKILL.md</files>
  <action>
Read the current SKILL.md first.

**Change 1 — frontmatter `references.required`** (currently lines 20-22):
Add `references/bilingual-output.md` as the second item:

```yaml
references:
  required:
    - references/expression-patterns.md
    - references/bilingual-output.md
```

**Change 2 — Workflow Step 3 (Generate Review Report)**, add opt-out check at the start of the step.

Insert this block immediately before the "Write the review report in the following locked structure" sentence:

```
- **Opt-out check:** Before generating the report, scan the user's original trigger prompt for any of these phrases (case-insensitive, exact phrase match): `english only`, `no bilingual`, `only english`, `不要中文`. If any phrase is detected: omit all `> **[Chinese]** ...` blockquotes from the report — produce English-only concerns, questions, and verdict. If none detected: include Chinese blockquotes as normal.
```

**No other changes.** The report format already uses `**[Chinese]**` (correct label). Paragraph markers (`<!-- Paragraph N -->`) are optional per `references/bilingual-output.md` for structured-output Skills where document structure already provides navigation — do not add them.

**Line budget check:** Current file is 228 lines. Additions add ~5 lines. Expected ~233 lines — within budget.
  </action>
  <verify>
    <automated>grep -n "bilingual-output.md" ".claude/skills/reviewer-simulation-skill/SKILL.md" && grep -n "Opt-out check" ".claude/skills/reviewer-simulation-skill/SKILL.md"</automated>
  </verify>
  <done>SKILL.md contains references/bilingual-output.md in references.required and an opt-out check step in Workflow Step 3.</done>
</task>

<task type="auto">
  <name>Task 3: logic-skill — fix label + add opt-out + reference</name>
  <files>.claude/skills/logic-skill/SKILL.md</files>
  <action>
Read the current SKILL.md first.

**Change 1 — frontmatter `references.required`** (currently line 22: `required: []`):
Replace with:

```yaml
references:
  required:
    - references/bilingual-output.md
  leaf_hints: []
```

**Change 2 — label fix**: Replace ALL occurrences of `**[中文]**` with `**[Chinese]**` throughout the file. Current occurrences are at:
- Line 167: `> **[中文]** 问题：...` in the locked report format block
- Line 187: `> **[中文]** ...` in the Notes section below the format

Replace both exactly. Do not change any surrounding text.

**Change 3 — Workflow Step 4 (Write Report)**, add opt-out check at the start of the step.

Insert this block immediately before the "Assemble the two-part report" sentence:

```
- **Opt-out check:** Before assembling the report, scan the user's original trigger prompt for any of these phrases (case-insensitive, exact phrase match): `english only`, `no bilingual`, `only english`, `不要中文`. If any phrase is detected: omit all `> **[Chinese]** ...` blockquotes from the report — produce English-only issue entries. If none detected: include Chinese blockquotes as normal.
```

**Change 4 — References section body** (currently says "None. Logic verification is a pure analysis task..."): Update the first paragraph to acknowledge the new reference:

Change:
```
None. Logic verification is a pure analysis task — no expression pattern leaves are needed, and anti-AI patterns are not loaded (consistent with Reviewer Simulation Skill convention). Logic verification is also journal-agnostic; no journal templates are loaded.
```

To:
```
Logic verification loads `references/bilingual-output.md` to govern the Chinese translation format and opt-out behavior. No expression pattern leaves are needed, and anti-AI patterns are not loaded (consistent with Reviewer Simulation Skill convention). Logic verification is also journal-agnostic; no journal templates are loaded.
```

**Line budget check:** Current file is 275 lines. Net change: ~+8 lines. Expected ~283 lines — within budget.
  </action>
  <verify>
    <automated>grep -n "bilingual-output.md" ".claude/skills/logic-skill/SKILL.md" && grep -n "\*\*\[Chinese\]\*\*" ".claude/skills/logic-skill/SKILL.md" && grep -c "\*\*\[中文\]\*\*" ".claude/skills/logic-skill/SKILL.md"</automated>
  </verify>
  <done>SKILL.md contains references/bilingual-output.md in references.required, zero occurrences of **[中文]** (all replaced with **[Chinese]**), and an opt-out check step in Workflow Step 4.</done>
</task>

<task type="auto">
  <name>Task 4: polish-skill — add bilingual conversation output + opt-out + reference</name>
  <files>.claude/skills/polish-skill/SKILL.md</files>
  <action>
Read the current SKILL.md first.

**Change 1 — frontmatter `references.required`**: Add `references/bilingual-output.md` as second item:

```yaml
references:
  required:
    - references/expression-patterns.md
    - references/bilingual-output.md
```

**Change 2 — frontmatter `output_contract`**: Add `bilingual_conversation`:

```yaml
output_contract:
  - polished_text
  - change_annotations
  - summary_report
  - bilingual_conversation
```

**Change 3 — Workflow, Quick-fix Step 1 (Collect Context)**: Add opt-out detection at the end of Step 1.

Append to the Step 1 bullet list:

```
- **Opt-out check:** Scan the user's trigger prompt for any of these phrases (case-insensitive, exact phrase match): `english only`, `no bilingual`, `only english`, `不要中文`. Store result as `bilingual_mode` (true/false). This flag governs Step 4 bilingual output below.
```

**Change 4 — Workflow, Quick-fix Step 4 (Summary)**: Add bilingual output block after the summary report.

After the existing Step 4 bullet ("Include 'Recommend running De-AI Skill...'"), add a new numbered step:

```
**Step 5 -- Bilingual Display (file input only):**
- If `bilingual_mode` is true and input was a file: for each paragraph that was modified in Step 2, display a `> **[Chinese]** ...` blockquote in conversation showing the Chinese translation of the polished English text.
- Use a section header in conversation: "**双语对照 / Bilingual Comparison:**" before the first blockquote.
- Format per paragraph:

  > **[Chinese]** [Chinese translation of the polished paragraph]

- Do not insert Chinese into the .tex file. The file remains English-only and submission-ready.
- If `bilingual_mode` is false (opt-out detected): skip this step entirely.
- Pasted text input: if `bilingual_mode` is true, append the `> **[Chinese]** ...` blockquote immediately after each polished paragraph in the conversation output.
```

**Change 5 — Guided Mode**: In Guided Mode Step 5 (Summary), add the same Step 6 bilingual display instruction referencing the same `bilingual_mode` flag and same format.

**Change 6 — Output Contract table**: Add new row:

| `bilingual_conversation` | `> **[Chinese]** ...` blockquotes in session | File input: modified paragraphs only. Pasted text: after each output paragraph. Skipped when opt-out detected. |

**Line budget check:** Current file is 197 lines. Additions add ~25 lines. Expected ~222 lines — within 300-line budget.
  </action>
  <verify>
    <automated>grep -n "bilingual-output.md" ".claude/skills/polish-skill/SKILL.md" && grep -n "bilingual_conversation" ".claude/skills/polish-skill/SKILL.md" && grep -n "Opt-out check" ".claude/skills/polish-skill/SKILL.md" && grep -n "双语对照" ".claude/skills/polish-skill/SKILL.md"</automated>
  </verify>
  <done>SKILL.md contains references/bilingual-output.md in references.required, bilingual_conversation in output_contract, opt-out check in Quick-fix Step 1, and bilingual display step in Quick-fix Step 5 and Guided Step 6.</done>
</task>

<task type="auto">
  <name>Task 5: de-ai-skill — add bilingual conversation output + opt-out + reference</name>
  <files>.claude/skills/de-ai-skill/SKILL.md</files>
  <action>
Read the current SKILL.md first.

**Change 1 — frontmatter `references.required`**: Add `references/bilingual-output.md` as third item (after the two existing entries):

```yaml
references:
  required:
    - references/anti-ai-patterns.md
    - references/expression-patterns.md
    - references/bilingual-output.md
```

**Change 2 — frontmatter `output_contract`**: Add `bilingual_conversation`:

```yaml
output_contract:
  - detection_report
  - rewritten_text
  - rewrite_report
  - bilingual_conversation
```

**Change 3 — Workflow Phase 1 Step 1 (Prepare)**: Add opt-out detection at the end of the step.

Append to the Phase 1 Step 1 bullet list:

```
- **Opt-out check:** Scan the user's trigger prompt for any of these phrases (case-insensitive, exact phrase match): `english only`, `no bilingual`, `only english`, `不要中文`. Store result as `bilingual_mode` (true/false). This flag governs Phase 2 bilingual output below.
```

**Change 4 — Workflow Phase 2 Step 3 (Rewrite Report)**: Add bilingual display block after the rewrite report table.

After the existing Step 3 table, add a new Step 4 (renaming the existing "Step 4 -- Summary" to "Step 5 -- Summary"):

```
**Step 4 -- Bilingual Display:**
- If `bilingual_mode` is true and input was a file: for each paragraph that was rewritten in Step 1, display a `> **[Chinese]** ...` blockquote in conversation showing the Chinese translation of the rewritten English text.
- Use a section header in conversation: "**双语对照 / Bilingual Comparison:**" before the first blockquote.
- Format per paragraph:

  > **[Chinese]** [Chinese translation of the rewritten paragraph]

- Do not insert Chinese into the .tex file. The file remains English-only and submission-ready.
- If `bilingual_mode` is false (opt-out detected): skip this step entirely.
- Pasted text input: if `bilingual_mode` is true, append the `> **[Chinese]** ...` blockquote immediately after each rewritten paragraph in the conversation diff output.
```

**Change 5 — Output Contract table**: Add new row:

| `bilingual_conversation` | `> **[Chinese]** ...` blockquotes in session | Phase 2 rewritten paragraphs only. Skipped when opt-out detected. |

**Line budget check:** Current file is 214 lines. Additions add ~22 lines. Expected ~236 lines — within 300-line budget.
  </action>
  <verify>
    <automated>grep -n "bilingual-output.md" ".claude/skills/de-ai-skill/SKILL.md" && grep -n "bilingual_conversation" ".claude/skills/de-ai-skill/SKILL.md" && grep -n "Opt-out check" ".claude/skills/de-ai-skill/SKILL.md" && grep -n "双语对照" ".claude/skills/de-ai-skill/SKILL.md"</automated>
  </verify>
  <done>SKILL.md contains references/bilingual-output.md in references.required, bilingual_conversation in output_contract, opt-out check in Phase 1 Step 1, and bilingual display as Phase 2 Step 4.</done>
</task>

<task type="auto">
  <name>Task 6: abstract-skill — add bilingual blockquote output + opt-out + reference</name>
  <files>.claude/skills/abstract-skill/SKILL.md</files>
  <action>
Read the current SKILL.md first.

**Change 1 — frontmatter `references.required`**: Add `references/bilingual-output.md` as second item:

```yaml
references:
  required:
    - references/expression-patterns.md
    - references/bilingual-output.md
```

**Change 2 — frontmatter `output_contract`**: Add `bilingual_abstract`:

```yaml
output_contract:
  - labeled_abstract
  - clean_abstract
  - bilingual_abstract
```

**Change 3 — Workflow Step 1 (Collect Context)**: Add opt-out detection at the end of the step.

Append to Step 1 bullet list:

```
- **Opt-out check:** Scan the user's trigger prompt for any of these phrases (case-insensitive, exact phrase match): `english only`, `no bilingual`, `only english`, `不要中文`. Store result as `bilingual_mode` (true/false). This flag governs Step 3 output below.
```

**Change 4 — Workflow Step 3 (Output)**: Add bilingual display after the labeled and clean versions.

After the existing "If the user wants to save: offer to write to a file" bullet, add:

```
- **Bilingual display:** If `bilingual_mode` is true: after presenting the labeled version and after presenting the clean version, append a `> **[Chinese]** ...` blockquote for each sentence of the abstract. Use a header: "**双语对照 / Bilingual Comparison:**" before the blockquotes. Format:

  > **[Chinese]** [1: 贡献] ...
  > **[Chinese]** [2: 难度] ...
  > **[Chinese]** [3: 方法] ...
  > **[Chinese]** [4: 证据] ...
  > **[Chinese]** [5: 关键结果] ...

  Each blockquote corresponds to one Farquhar formula sentence. Label prefixes in Chinese are for readability only — the clean version Chinese follows the plain paragraph structure without labels.
- If `bilingual_mode` is false (opt-out detected): skip bilingual display entirely.
```

**Change 5 — Output Contract table**: Add new row:

| `bilingual_abstract` | `> **[Chinese]** ...` blockquotes in session (one per sentence) | When bilingual_mode is true (default). Skipped when opt-out detected. |

**Line budget check:** Current file is 215 lines. Additions add ~20 lines. Expected ~235 lines — within 300-line budget.
  </action>
  <verify>
    <automated>grep -n "bilingual-output.md" ".claude/skills/abstract-skill/SKILL.md" && grep -n "bilingual_abstract" ".claude/skills/abstract-skill/SKILL.md" && grep -n "Opt-out check" ".claude/skills/abstract-skill/SKILL.md" && grep -n "双语对照" ".claude/skills/abstract-skill/SKILL.md"</automated>
  </verify>
  <done>SKILL.md contains references/bilingual-output.md in references.required, bilingual_abstract in output_contract, opt-out check in Step 1, and bilingual display in Step 3.</done>
</task>

<task type="auto">
  <name>Task 7: experiment-skill — add bilingual blockquote output + opt-out + reference</name>
  <files>.claude/skills/experiment-skill/SKILL.md</files>
  <action>
Read the current SKILL.md first.

**Change 1 — frontmatter `references.required`**: Add `references/bilingual-output.md` as second item:

```yaml
references:
  required:
    - references/expression-patterns.md
    - references/bilingual-output.md
```

**Change 2 — frontmatter `output_contract`**: Add `bilingual_discussion`:

```yaml
output_contract:
  - pattern_analysis
  - discussion_paragraphs
  - bilingual_discussion
```

**Change 3 — Phase 1 Step 1 (Prepare)**: Add opt-out detection at the end of the step.

Append to Phase 1 Step 1 bullet list:

```
- **Opt-out check:** Scan the user's trigger prompt for any of these phrases (case-insensitive, exact phrase match): `english only`, `no bilingual`, `only english`, `不要中文`. Store result as `bilingual_mode` (true/false). This flag governs Phase 2 bilingual output below.
```

**Change 4 — Phase 2 Step 3 (Output)**: Add bilingual display block.

After the existing "Present all discussion paragraphs in sequence" bullet, add:

```
- **Bilingual display:** If `bilingual_mode` is true: after each discussion paragraph, append a `> **[Chinese]** ...` blockquote containing the Chinese translation of that paragraph. Use a section header "**双语对照 / Bilingual Comparison:**" before the first paragraph. Format per finding paragraph:

  [English discussion paragraph for Finding N]

  > **[Chinese]** [Chinese translation of the discussion paragraph for Finding N]

- Do not insert Chinese into any written file. If the user requested writing discussion to the paper file via Write tool, write English-only paragraphs to the file; the Chinese blockquotes remain in conversation only.
- If `bilingual_mode` is false (opt-out detected): skip bilingual display entirely.
```

**Change 5 — Output Contract table**: Add new row:

| `bilingual_discussion` | `> **[Chinese]** ...` blockquotes in session (one per finding paragraph) | Phase 2 only. Skipped when opt-out detected. Not written to file. |

**Line budget check:** Current file is 231 lines. Additions add ~22 lines. Expected ~253 lines — within 300-line budget.
  </action>
  <verify>
    <automated>grep -n "bilingual-output.md" ".claude/skills/experiment-skill/SKILL.md" && grep -n "bilingual_discussion" ".claude/skills/experiment-skill/SKILL.md" && grep -n "Opt-out check" ".claude/skills/experiment-skill/SKILL.md" && grep -n "双语对照" ".claude/skills/experiment-skill/SKILL.md"</automated>
  </verify>
  <done>SKILL.md contains references/bilingual-output.md in references.required, bilingual_discussion in output_contract, opt-out check in Phase 1 Step 1, and bilingual display in Phase 2 Step 3.</done>
</task>

</tasks>

<verification>
After all 7 tasks complete, run these cross-cutting checks:

1. All 7 files reference bilingual-output.md:
   `grep -l "bilingual-output.md" .claude/skills/*/SKILL.md | wc -l`
   Expected: 7

2. All 7 files have opt-out check:
   `grep -l "Opt-out check" .claude/skills/translation-skill/SKILL.md .claude/skills/reviewer-simulation-skill/SKILL.md .claude/skills/logic-skill/SKILL.md .claude/skills/polish-skill/SKILL.md .claude/skills/de-ai-skill/SKILL.md .claude/skills/abstract-skill/SKILL.md .claude/skills/experiment-skill/SKILL.md | wc -l`
   Expected: 7

3. Zero occurrences of old Chinese label in logic-skill:
   `grep -c "\*\*\[中文\]\*\*" .claude/skills/logic-skill/SKILL.md`
   Expected: 0

4. No SKILL.md exceeds 300 lines:
   `wc -l .claude/skills/translation-skill/SKILL.md .claude/skills/reviewer-simulation-skill/SKILL.md .claude/skills/logic-skill/SKILL.md .claude/skills/polish-skill/SKILL.md .claude/skills/de-ai-skill/SKILL.md .claude/skills/abstract-skill/SKILL.md .claude/skills/experiment-skill/SKILL.md`
   Expected: all values <= 300
</verification>

<success_criteria>
- All 7 SKILL.md files contain `references/bilingual-output.md` in their `references.required` frontmatter field
- All 7 SKILL.md files contain an opt-out detection step that checks for `english only`, `no bilingual`, `only english`, `不要中文`
- logic-skill has zero occurrences of `**[中文]**`; all replaced with `**[Chinese]**`
- polish-skill and de-ai-skill output contract includes `bilingual_conversation`; workflow shows `> **[Chinese]** ...` blockquote display per modified paragraph after edits, English-only in file
- abstract-skill output contract includes `bilingual_abstract`; workflow shows per-sentence bilingual blockquotes in Step 3
- experiment-skill output contract includes `bilingual_discussion`; workflow shows per-paragraph bilingual blockquotes in Phase 2 Step 3, not written to file
- translation-skill and reviewer-simulation-skill: output contract updated to note opt-out exception; no bilingual format changes otherwise
- No SKILL.md file exceeds 300 lines
</success_criteria>

<output>
After completion, create `.planning/phases/17-existing-skills-bilingual-update/17-01-SUMMARY.md` following the summary template at `@C:/Users/Lylll/.claude/get-shit-done/templates/summary.md`.
</output>
