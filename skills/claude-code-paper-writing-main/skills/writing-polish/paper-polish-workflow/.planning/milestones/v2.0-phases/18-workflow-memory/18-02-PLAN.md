---
phase: 18-workflow-memory
plan: 02
type: execute
wave: 2
depends_on:
  - 18-01
files_modified:
  - .claude/skills/translation-skill/SKILL.md
  - .claude/skills/polish-skill/SKILL.md
  - .claude/skills/de-ai-skill/SKILL.md
  - .claude/skills/reviewer-simulation-skill/SKILL.md
  - .claude/skills/abstract-skill/SKILL.md
  - .claude/skills/experiment-skill/SKILL.md
  - .claude/skills/logic-skill/SKILL.md
  - .claude/skills/caption-skill/SKILL.md
  - .claude/skills/cover-letter-skill/SKILL.md
  - .claude/skills/visualization-skill/SKILL.md
  - .claude/skills/literature-skill/SKILL.md
  - .claude/skills/repo-to-paper-skill/SKILL.md
autonomous: true
requirements:
  - UXFIX-03

must_haves:
  truths:
    - "All 12 Skills have a Step 0 Workflow Memory Check at the start of their Workflow section"
    - "All 12 Skills have a record-write step after their first-step validation completes"
    - "Step 0 references skill-conventions.md for the full algorithm"
    - "Record-write uses the correct skill name matching the SKILL.md name field"
    - "No Skill exceeds its line budget after modifications (300 lines, except repo-to-paper which has existing justification)"
  artifacts:
    - path: ".claude/skills/translation-skill/SKILL.md"
      provides: "Workflow memory integration"
      contains: "Step 0"
    - path: ".claude/skills/polish-skill/SKILL.md"
      provides: "Workflow memory integration"
      contains: "Step 0"
    - path: ".claude/skills/de-ai-skill/SKILL.md"
      provides: "Workflow memory integration"
      contains: "Step 0"
    - path: ".claude/skills/reviewer-simulation-skill/SKILL.md"
      provides: "Workflow memory integration"
      contains: "Step 0"
    - path: ".claude/skills/abstract-skill/SKILL.md"
      provides: "Workflow memory integration"
      contains: "Step 0"
    - path: ".claude/skills/experiment-skill/SKILL.md"
      provides: "Workflow memory integration"
      contains: "Step 0"
    - path: ".claude/skills/logic-skill/SKILL.md"
      provides: "Workflow memory integration"
      contains: "Step 0"
    - path: ".claude/skills/caption-skill/SKILL.md"
      provides: "Workflow memory integration"
      contains: "Step 0"
    - path: ".claude/skills/cover-letter-skill/SKILL.md"
      provides: "Workflow memory integration"
      contains: "Step 0"
    - path: ".claude/skills/visualization-skill/SKILL.md"
      provides: "Workflow memory integration"
      contains: "Step 0"
    - path: ".claude/skills/literature-skill/SKILL.md"
      provides: "Workflow memory integration"
      contains: "Step 0"
    - path: ".claude/skills/repo-to-paper-skill/SKILL.md"
      provides: "Workflow memory integration"
      contains: "Step 0"
  key_links:
    - from: ".claude/skills/*/SKILL.md"
      to: ".planning/workflow-memory.json"
      via: "Step 0 reads and record-write appends"
      pattern: "workflow-memory\\.json"
    - from: ".claude/skills/*/SKILL.md"
      to: "references/skill-conventions.md"
      via: "Step 0 references conventions for full algorithm"
      pattern: "skill-conventions\\.md.*Workflow Memory"
---

<objective>
Add Workflow Memory integration (Step 0 pattern check + record-write step) to all 12 existing Skills.

Purpose: Every Skill now participates in the workflow memory system, recording invocations and checking for frequent patterns to offer recommendations.
Output: All 12 SKILL.md files updated with Step 0 and record-write step.
</objective>

<execution_context>
@C:/Users/Lylll/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Lylll/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-workflow-memory/18-CONTEXT.md
@.planning/phases/18-workflow-memory/18-RESEARCH.md
@.planning/phases/18-workflow-memory/18-01-SUMMARY.md
</context>

<interfaces>
<!-- From Plan 01 outputs — the convention rules that all Skills must follow -->

From references/skill-conventions.md (after Plan 01 update):
- ## Workflow Memory section defines recording rules and pattern detection algorithm
- Recording: append after first-step validation, entry format {"skill": "<skill-name>", "ts": "<ISO timestamp>"}, 50-entry cap
- Pattern Detection (Step 0): read history at startup, check 2/3-item patterns >= threshold, AskUserQuestion recommendation

From references/skill-skeleton.md (after Plan 01 update):
- ### Step 0: Workflow Memory Check — template for pattern detection
- Record-write line in Step 1 — template for history recording
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Add Workflow Memory to 6 standard-structure Skills (translation, polish, reviewer-simulation, abstract, caption, cover-letter)</name>
  <files>
    .claude/skills/translation-skill/SKILL.md,
    .claude/skills/polish-skill/SKILL.md,
    .claude/skills/reviewer-simulation-skill/SKILL.md,
    .claude/skills/abstract-skill/SKILL.md,
    .claude/skills/caption-skill/SKILL.md,
    .claude/skills/cover-letter-skill/SKILL.md
  </files>
  <read_first>
    - .claude/skills/translation-skill/SKILL.md
    - .claude/skills/polish-skill/SKILL.md
    - .claude/skills/reviewer-simulation-skill/SKILL.md
    - .claude/skills/abstract-skill/SKILL.md
    - .claude/skills/caption-skill/SKILL.md
    - .claude/skills/cover-letter-skill/SKILL.md
    - references/skill-conventions.md (Workflow Memory section — the source of truth for the algorithm)
    - references/skill-skeleton.md (Step 0 template and record-write template)
    - .planning/phases/18-workflow-memory/18-RESEARCH.md (insertion point table for each Skill)
  </read_first>
  <action>
For each of the 6 Skills below, make two additions to the `## Workflow` section. Keep additions concise (~10-12 lines total per Skill) to stay within the 300-line budget.

**Addition 1: Step 0 — Workflow Memory Check**

Insert a new subsection at the very start of `## Workflow`, BEFORE the first existing step. Use this exact template, replacing `[skill-name]` with the Skill's actual `name` field:

```markdown
### Step 0: Workflow Memory Check

- Read `.planning/workflow-memory.json`. If file missing or empty, skip to Step 1.
- Check if the last 1-2 log entries form a recognized pattern with `[skill-name]` that has appeared >= threshold times in the log. See `skill-conventions.md > Workflow Memory > Pattern Detection` for the full algorithm.
- If a pattern is found, present recommendation via AskUserQuestion:
  - Question: "检测到常用流程：[pattern]（已出现 N 次）。是否直接以 direct 模式运行 [skill-name]？"
  - Options: "Yes, proceed" / "No, continue normally"
- If user accepts: set mode to `direct`, skip Ask Strategy questions.
- If user declines or AskUserQuestion unavailable: continue in normal mode.
```

**Addition 2: Record-write after first-step validation**

Append a single line at the end of the Skill's first validation step. The exact insertion point per Skill:

| Skill | Insert record-write after |
|---|---|
| translation-skill | End of `### Step 1: Collect Context` (after all reference loading and detection logic) |
| polish-skill | End of `**Step 1 -- Collect Context:**` in BOTH Quick-fix AND Guided modes. For Guided: note "Same as Quick-fix Step 1" already exists, so add the record-write to Quick-fix Step 1 and it covers both. |
| reviewer-simulation-skill | End of `### Step 1: Collect Context` |
| abstract-skill | End of `### Step 1: Collect Context` |
| caption-skill | End of `### Step 1: Collect Context` |
| cover-letter-skill | End of `### Step 1: Collect Context` |

The record-write line to add:

```markdown
- **Record workflow:** Append `{"skill": "[skill-name]", "ts": "<ISO timestamp>"}` to `.planning/workflow-memory.json`. Create file as `[]` if missing. Drop oldest entry if log length >= 50.
```

Replace `[skill-name]` with the actual name: `translation-skill`, `polish-skill`, `reviewer-simulation-skill`, `abstract-skill`, `caption-skill`, `cover-letter-skill`.

**Special note for polish-skill:** polish-skill has two modes (Quick-fix and Guided) with separate Step 1 descriptions. The Quick-fix `Step 1 -- Collect Context` is the primary one. Guided `Step 1 -- Collect Context` says "Same as Quick-fix Step 1, plus confirm scope with user." The record-write goes into Quick-fix Step 1, and since Guided references it, it is inherited. Do NOT add a second record-write to Guided Step 1.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const skills=['translation','polish','reviewer-simulation','abstract','caption','cover-letter']; const errors=[]; skills.forEach(s=>{const p='.claude/skills/'+s+'-skill/SKILL.md'; const c=fs.readFileSync(p,'utf8'); if(!c.includes('Step 0: Workflow Memory Check')) errors.push(s+': missing Step 0'); if(!c.includes('Record workflow:')) errors.push(s+': missing record-write'); const lines=c.split('\n').length; if(lines>300 && s!=='repo-to-paper') errors.push(s+': '+lines+' lines exceeds budget');}); if(errors.length){console.log(errors.join('\n'));process.exit(1)} console.log('All 6 standard Skills OK');"</automated>
  </verify>
  <acceptance_criteria>
    - Each of the 6 Skills contains `### Step 0: Workflow Memory Check` before the first existing step
    - Each Step 0 contains `workflow-memory.json`, `AskUserQuestion`, and `skill-conventions.md > Workflow Memory`
    - Each of the 6 Skills contains `Record workflow:` with the correct skill name (e.g., `"skill": "polish-skill"`)
    - Record-write is placed AFTER Step 1 / Collect Context completion, not at invocation time
    - No Skill exceeds 300 lines (check with `wc -l`)
    - All pre-existing Skill content is preserved
  </acceptance_criteria>
  <done>6 standard-structure Skills have Step 0 and record-write integrated correctly</done>
</task>

<task type="auto">
  <name>Task 2: Add Workflow Memory to 6 non-standard-structure Skills (de-ai, experiment, logic, visualization, literature, repo-to-paper)</name>
  <files>
    .claude/skills/de-ai-skill/SKILL.md,
    .claude/skills/experiment-skill/SKILL.md,
    .claude/skills/logic-skill/SKILL.md,
    .claude/skills/visualization-skill/SKILL.md,
    .claude/skills/literature-skill/SKILL.md,
    .claude/skills/repo-to-paper-skill/SKILL.md
  </files>
  <read_first>
    - .claude/skills/de-ai-skill/SKILL.md
    - .claude/skills/experiment-skill/SKILL.md
    - .claude/skills/logic-skill/SKILL.md
    - .claude/skills/visualization-skill/SKILL.md
    - .claude/skills/literature-skill/SKILL.md
    - .claude/skills/repo-to-paper-skill/SKILL.md
    - references/skill-conventions.md (Workflow Memory section)
    - references/skill-skeleton.md (Step 0 template)
    - .planning/phases/18-workflow-memory/18-RESEARCH.md (insertion point table and repo-to-paper special handling)
  </read_first>
  <action>
For each of the 6 Skills below, make two additions. These Skills have non-standard Workflow structures (phases instead of steps, different first-step names, or special considerations).

**Addition 1: Step 0 — Workflow Memory Check**

Insert `### Step 0: Workflow Memory Check` at the very start of `## Workflow`, BEFORE the first existing step/phase. Use the same template as Task 1, replacing `[skill-name]` appropriately:

```markdown
### Step 0: Workflow Memory Check

- Read `.planning/workflow-memory.json`. If file missing or empty, skip to [first step name].
- Check if the last 1-2 log entries form a recognized pattern with `[skill-name]` that has appeared >= threshold times in the log. See `skill-conventions.md > Workflow Memory > Pattern Detection` for the full algorithm.
- If a pattern is found, present recommendation via AskUserQuestion:
  - Question: "检测到常用流程：[pattern]（已出现 N 次）。是否直接以 direct 模式运行 [skill-name]？"
  - Options: "Yes, proceed" / "No, continue normally"
- If user accepts: set mode to `direct`, skip Ask Strategy questions.
- If user declines or AskUserQuestion unavailable: continue in normal mode.
```

Replace `[first step name]` with: "Phase 1" for de-ai and experiment, "Step 1" for logic/visualization/literature/repo-to-paper.

**Special handling for repo-to-paper-skill:** repo-to-paper does not support `direct` mode. If the user accepts the recommendation, the behavior is: skip the 2 Ask Strategy questions (repo path + journal) if they can be inferred from context or prior invocation, but KEEP all guided Step checkpoints (Steps 2-5 confirmations are inherent to the output contract). Add this note to repo-to-paper's Step 0:

```markdown
- **Note:** This Skill does not support full `direct` mode. If user accepts the recommendation, skip the Ask Strategy questions (repo path, journal) using inferred context, but retain all guided Step checkpoints (Steps 2-5 confirmations). The output contract is not altered.
```

**Addition 2: Record-write after first-step validation**

The exact insertion point differs per Skill:

| Skill | First step name | Insert record-write after |
|---|---|---|
| de-ai-skill | Phase 1, Step 1 -- Prepare | End of `**Step 1 -- Prepare:**` under `### Phase 1: Detect` (after all reference loading, input reading, and opt-out check) |
| experiment-skill | Phase 1, Step 1 -- Prepare | End of `**Step 1 — Prepare:**` under `### Phase 1: Analyze Results` (after guard check passes and input is read) |
| logic-skill | Step 1 — Input Guard | End of `### Step 1 — Input Guard` (after full-paper guard passes and section structure is parsed) |
| visualization-skill | Step 1 — Collect Context | End of `### Step 1 — Collect Context` |
| literature-skill | Step 2: Collect Search Query | End of `### Step 2: Collect Search Query` (Step 1 is MCP pre-flight which may fail; record only after query is confirmed in Step 2) |
| repo-to-paper-skill | Step 1: Scan Repository | End of `### Step 1: Scan Repository` (after repo is scanned and categorized summary is displayed) |

The record-write line to add (same format, different skill names):

```markdown
- **Record workflow:** Append `{"skill": "[skill-name]", "ts": "<ISO timestamp>"}` to `.planning/workflow-memory.json`. Create file as `[]` if missing. Drop oldest entry if log length >= 50.
```

Replace `[skill-name]` with: `de-ai-skill`, `experiment-skill`, `logic-skill`, `visualization-skill`, `literature-skill`, `repo-to-paper-skill`.

**Important for literature-skill:** The record-write goes after Step 2 (Collect Search Query), NOT after Step 1 (MCP Pre-flight). Step 1 is a pre-flight check that may fail and refuse the Skill. If MCP is unavailable and the Skill refuses, no record should be written. Only after Step 2 confirms the search query does the Skill begin producing output.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const skills=['de-ai','experiment','logic','visualization','literature','repo-to-paper']; const errors=[]; skills.forEach(s=>{const p='.claude/skills/'+s+'-skill/SKILL.md'; const c=fs.readFileSync(p,'utf8'); if(!c.includes('Step 0: Workflow Memory Check')) errors.push(s+': missing Step 0'); if(!c.includes('Record workflow:')) errors.push(s+': missing record-write'); const lines=c.split('\n').length; if(lines>300 && s!=='repo-to-paper') errors.push(s+': '+lines+' lines exceeds budget');}); if(errors.length){console.log(errors.join('\n'));process.exit(1)} console.log('All 6 non-standard Skills OK');"</automated>
  </verify>
  <acceptance_criteria>
    - Each of the 6 Skills contains `### Step 0: Workflow Memory Check` before the first existing step/phase
    - Each Step 0 contains `workflow-memory.json`, `AskUserQuestion`, and `skill-conventions.md > Workflow Memory`
    - de-ai-skill record-write is inside Phase 1 Step 1 (after Prepare), NOT at invocation
    - experiment-skill record-write is inside Phase 1 Step 1 (after Prepare), NOT at invocation
    - logic-skill record-write is after Step 1 Input Guard passes
    - visualization-skill record-write is after Step 1 Collect Context
    - literature-skill record-write is after Step 2 Collect Search Query (NOT after Step 1 MCP pre-flight)
    - repo-to-paper-skill record-write is after Step 1 Scan Repository completes
    - repo-to-paper-skill Step 0 contains the special note about retaining guided Step checkpoints
    - Each record-write uses the correct skill name (e.g., `"skill": "de-ai-skill"`)
    - No Skill exceeds 300 lines except repo-to-paper (which has existing justification for exceeding)
  </acceptance_criteria>
  <done>6 non-standard-structure Skills have Step 0 and record-write integrated correctly, with repo-to-paper special handling documented</done>
</task>

</tasks>

<verification>
1. All 12 Skills contain `Step 0: Workflow Memory Check`:
   `grep -rl "Step 0: Workflow Memory Check" .claude/skills/*/SKILL.md | wc -l` returns 12

2. All 12 Skills contain `Record workflow:`:
   `grep -rl "Record workflow:" .claude/skills/*/SKILL.md | wc -l` returns 12

3. Each record-write uses the matching skill name:
   `for d in .claude/skills/*/SKILL.md; do name=$(grep '^name:' "$d" | sed 's/name: //'); if ! grep -q "\"skill\": \"$name\"" "$d"; then echo "MISMATCH: $d"; fi; done`

4. Line budget check:
   `wc -l .claude/skills/*/SKILL.md | grep -v 'repo-to-paper' | awk '$1>300{print "OVER BUDGET:", $0}'`

5. repo-to-paper special note present:
   `grep -c "retain all guided Step checkpoints" .claude/skills/repo-to-paper-skill/SKILL.md` returns 1
</verification>

<success_criteria>
- All 12 SKILL.md files have Step 0 Workflow Memory Check at the start of their Workflow section
- All 12 SKILL.md files have record-write after first-step validation at the correct insertion point
- repo-to-paper-skill has special direct-mode handling documented in Step 0
- literature-skill record-write is after Step 2 (not Step 1)
- No Skill exceeds 300 lines (except repo-to-paper with existing justification)
- All record-write entries use the correct skill name matching the frontmatter name field
</success_criteria>

<output>
After completion, create `.planning/phases/18-workflow-memory/18-02-SUMMARY.md`
</output>
