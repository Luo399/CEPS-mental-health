---
phase: 11-convention-tech-debt
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - references/skill-conventions.md
  - references/skill-skeleton.md
autonomous: true
requirements: [DEBT-01, DEBT-02, DEBT-03, UXFIX-02]

must_haves:
  truths:
    - "skill-conventions.md escape hatch rule names logic-skill, visualization-skill, and literature-skill as concrete examples"
    - "skill-conventions.md contains AskUserQuestion enforcement subsection with good/bad comparison, direct-mode exemption, and per-Skill audit approach"
    - "skill-conventions.md contains bilingual output eligibility classification listing 7 eligible and 4 exempt Skills"
    - "literature-skill SKILL.md tools field reads 'External MCP' (verify-only, no edit)"
    - "cover-letter-skill SKILL.md required field lists references/journals/ceus.md (verify-only, no edit)"
    - "skill-skeleton.md stays in sync with new conventions content"
  artifacts:
    - path: "references/skill-conventions.md"
      provides: "Updated conventions with escape hatch enhancement, AskUserQuestion enforcement, bilingual eligibility"
      contains: "logic-skill"
    - path: "references/skill-skeleton.md"
      provides: "Skeleton synced with new AskUserQuestion and bilingual notes"
      contains: "AskUserQuestion"
  key_links:
    - from: "references/skill-conventions.md"
      to: "references/skill-skeleton.md"
      via: "Maintenance Rules line 194 sync requirement"
      pattern: "Keep the example skeleton in sync"
    - from: "references/skill-conventions.md"
      to: ".claude/skills/*/SKILL.md"
      via: "All Skills follow conventions as authoring rules"
      pattern: "Canonical authoring rules"
---

<objective>
Update skill-conventions.md with three additions (escape hatch Skill name references, AskUserQuestion enforcement rule, bilingual output eligibility classification), sync skill-skeleton.md, and verify two previously-applied fixes (DEBT-02, DEBT-03).

Purpose: Resolve all v1.0 tech debt and update conventions so v2.0 authoring starts from a clean, consistent foundation.
Output: Updated `references/skill-conventions.md`, synced `references/skill-skeleton.md`, verification evidence for DEBT-02/DEBT-03.
</objective>

<execution_context>
@C:/Users/Lylll/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Lylll/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-convention-tech-debt/11-CONTEXT.md
@.planning/phases/11-convention-tech-debt/11-RESEARCH.md

<interfaces>
<!-- Current skill-conventions.md structure (199 lines): -->
<!-- Line 73: Escape hatch rule for `required: []` — DEBT-01 enhancement target -->
<!-- Lines 159-177: `## Ask Strategy Conventions` section — UXFIX-02 expansion target -->
<!-- Lines 36-49: Body Structure table — bilingual eligibility goes after this section -->
<!-- Lines 189-195: `## Maintenance Rules` — skeleton sync rule at line 194 -->

<!-- Current skill-skeleton.md structure (155 lines): -->
<!-- Lines 90-101: `## Ask Strategy` section — needs AskUserQuestion enforcement note -->
<!-- Lines 126-133: `## Output Contract` section — needs bilingual eligibility note -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update skill-conventions.md with escape hatch enhancement, AskUserQuestion enforcement, and bilingual eligibility</name>
  <files>references/skill-conventions.md</files>
  <read_first>
    - references/skill-conventions.md (full current state — 199 lines, all edit locations)
    - .planning/phases/11-convention-tech-debt/11-CONTEXT.md (locked decisions for exact content requirements)
    - .planning/phases/11-convention-tech-debt/11-RESEARCH.md (code examples at Examples 1-3, AskUserQuestion schema reference)
    - .claude/skills/logic-skill/SKILL.md (confirm `required: []` usage for DEBT-01 reference)
    - .claude/skills/visualization-skill/SKILL.md (confirm `required: []` usage for DEBT-01 reference)
    - .claude/skills/literature-skill/SKILL.md (confirm `required: []` usage for DEBT-01 reference)
  </read_first>
  <action>
Make three edits to `references/skill-conventions.md`:

**Edit 1 — DEBT-01: Enhance escape hatch at line 73**

Replace the current line 73 text:
```
- **`required: []` is acceptable** only for self-contained Skills that produce no written academic text and load no local reference files by design. Qualifying cases: pure analysis Skills (e.g., logic checking), pure recommendation Skills (e.g., chart type suggestions), and external-MCP-only Skills (e.g., literature search). The `## References` body section must document why no reference loading is needed.
```

With this enhanced version that adds specific Skill name references inline:
```
- **`required: []` is acceptable** only for self-contained Skills that produce no written academic text and load no local reference files by design. Qualifying cases: pure analysis Skills (e.g., `logic-skill` — logic checking), pure recommendation Skills (e.g., `visualization-skill` — chart type suggestions), and external-MCP-only Skills (e.g., `literature-skill` — literature search). The `## References` body section must document why no reference loading is needed.
```

The change is minimal: insert backtick-quoted Skill names before each parenthetical example. Keep inline, do NOT create a subsection.

**Edit 2 — UXFIX-02: Add AskUserQuestion enforcement subsection after line 177**

After the existing `### When to Skip Asking` block (which ends at line 177), add a new subsection within the `## Ask Strategy Conventions` section. Insert the following content before the `---` divider that separates Ask Strategy from Naming and Discovery:

```markdown

### AskUserQuestion Enforcement

Each Skill's `## Ask Strategy` section declares which questions use `AskUserQuestion` and which use plain text. There is no blanket mandate — authors decide per-Skill based on interaction needs.

**Rules:**

- When `AskUserQuestion` is available and the user must choose between 2-4 discrete options, use it.
- In `direct` mode, Skills skip all `AskUserQuestion` calls entirely. The user chose direct = they want speed. Proceed with defaults and inferred context.
- In `batch` mode, `AskUserQuestion` calls are also skipped; settings from the first item apply to all subsequent items.
- When `AskUserQuestion` is unavailable, fall back per the existing rule in `## Fallback Rules > Structured Interaction Unavailable`.

**Good — structured question for multi-option selection:**

```
AskUserQuestion({
  question: "Which section should I polish first?",
  options: [
    { label: "Abstract", description: "Full abstract rewrite with word count check" },
    { label: "Introduction", description: "Logic flow and contribution statement" },
    { label: "Methods", description: "Technical accuracy and reproducibility" }
  ]
})
```

**Bad — plain dialogue for multi-option questions:**

```
"Which section should I polish first?
1. Abstract
2. Introduction
3. Methods
Please type the number."
```

Do not present options as numbered plain text when `AskUserQuestion` is available. The structured tool provides a timed multiple-choice interface that is faster and less error-prone.
```

**Edit 3 — Bilingual eligibility classification subsection after Body Structure table**

After the Body Structure table section (after the `### Section Depth` block that ends around line 55), add a new subsection before the `---` divider:

```markdown

### Bilingual Output Eligibility

Skills are classified as bilingual-eligible based on their `output_contract` type. Skills that produce academic text support bilingual paragraph-by-paragraph comparison; pure analysis, recommendation, and citation Skills are exempt.

| Eligibility | Skills | Reason |
|-------------|--------|--------|
| Eligible (default ON) | translation, polish, de-ai, reviewer-simulation, abstract, experiment, logic | Produces academic text where Chinese comparison aids the user |
| Exempt | caption, cover-letter, visualization, literature | English-only captions, journal-specific format, recommendation-only, BibTeX output |

Eligible Skills support bilingual paragraph-by-paragraph comparison output. Bilingual mode is ON by default; users may opt out. The bilingual format specification is defined in `references/bilingual-output.md` (created in Phase 13).
```

**Formatting rules:**
- Use `---` section dividers consistent with the rest of the document
- Use `###` for subsections within existing `##` sections
- Use backtick-quoted file paths for cross-references
- Ensure no duplicate guidance conflicts with existing sections (especially lines 108-114 Fallback Rules)
  </action>
  <verify>
    <automated>bash -c "grep -c 'logic-skill' references/skill-conventions.md && grep -c 'AskUserQuestion' references/skill-conventions.md && grep -c 'Bilingual Output Eligibility' references/skill-conventions.md && grep -c 'visualization-skill' references/skill-conventions.md && grep -c 'literature-skill' references/skill-conventions.md"</automated>
  </verify>
  <acceptance_criteria>
    - `references/skill-conventions.md` line 73 area contains `logic-skill`, `visualization-skill`, and `literature-skill` in backtick-quoted form
    - `grep -c "logic-skill" references/skill-conventions.md` returns >= 1
    - `grep -c "visualization-skill" references/skill-conventions.md` returns >= 1
    - `grep -c "literature-skill" references/skill-conventions.md` returns >= 1
    - `grep -c "AskUserQuestion Enforcement" references/skill-conventions.md` returns 1
    - `grep -c "AskUserQuestion" references/skill-conventions.md` returns >= 5 (enforcement heading, rules, good example, bad context, fallback cross-ref)
    - `grep -c "Bilingual Output Eligibility" references/skill-conventions.md` returns 1
    - `grep "direct mode" references/skill-conventions.md` matches the AskUserQuestion direct-mode exemption line
    - `grep "batch mode" references/skill-conventions.md` matches the batch-mode note
    - Document total is approximately 240-260 lines (199 original + ~45-60 added lines)
    - No `##` top-level sections added (all additions are `###` subsections within existing `##` sections)
    - No references to `mcp_question` in newly added text
  </acceptance_criteria>
  <done>
    skill-conventions.md contains: (1) escape hatch with three specific Skill names, (2) AskUserQuestion enforcement subsection with good/bad comparison, direct-mode and batch-mode exemptions, per-Skill audit approach, and fallback cross-reference, (3) bilingual output eligibility classification with 7 eligible and 4 exempt Skills in a table
  </done>
</task>

<task type="auto">
  <name>Task 2: Sync skill-skeleton.md and verify DEBT-02/DEBT-03 fixes</name>
  <files>references/skill-skeleton.md</files>
  <read_first>
    - references/skill-skeleton.md (full current state — 155 lines)
    - references/skill-conventions.md (read AFTER Task 1 edits to see final state for sync)
    - .claude/skills/literature-skill/SKILL.md (read-only verification for DEBT-02: line 16 should show `- External MCP`)
    - .claude/skills/cover-letter-skill/SKILL.md (read-only verification for DEBT-03: lines 21-22 should show `required:` with `- references/journals/ceus.md`)
  </read_first>
  <action>
**Part A — Sync skill-skeleton.md (2 edits)**

**Edit 1: Add AskUserQuestion note to Ask Strategy section**

In `references/skill-skeleton.md`, the `## Ask Strategy` section is at lines 90-101. After line 100 (`- Use structured interaction when available; fall back to plain-text questions otherwise.`), add the following line:

```
- See `skill-conventions.md > AskUserQuestion Enforcement` for when to use `AskUserQuestion` vs plain text.
```

This goes inside the existing `**Rules:**` bullet list, as a new final bullet.

**Edit 2: Add bilingual eligibility note to Output Contract section**

In `references/skill-skeleton.md`, the `## Output Contract` section is at lines 126-133. After the table (which ends around line 133), add:

```markdown

> **Bilingual eligibility:** If this Skill's `output_contract` produces academic text, it must support bilingual paragraph-by-paragraph comparison (ON by default). See `skill-conventions.md > Bilingual Output Eligibility` for the full classification.
```

**Part B — Verify DEBT-02 and DEBT-03 (read-only, NO edits)**

Verify DEBT-02: Read `.claude/skills/literature-skill/SKILL.md` and confirm line 16 contains `- External MCP` under the `tools:` field. Do NOT edit this file.

Verify DEBT-03: Read `.claude/skills/cover-letter-skill/SKILL.md` and confirm lines 21-22 contain `required:` followed by `- references/journals/ceus.md`. Do NOT edit this file.

Report verification results in the summary. If either verification fails (the expected text is not present), flag it as a blocker — but based on pre-planning checks, both are confirmed correct.

**IMPORTANT:** Do not modify `literature-skill/SKILL.md` or `cover-letter-skill/SKILL.md`. These are verify-only. `git diff` at the end should show changes ONLY in `references/skill-conventions.md` and `references/skill-skeleton.md`.
  </action>
  <verify>
    <automated>bash -c "grep 'AskUserQuestion Enforcement' references/skill-skeleton.md && grep 'Bilingual eligibility' references/skill-skeleton.md && grep 'External MCP' .claude/skills/literature-skill/SKILL.md && grep -A1 'required:' .claude/skills/cover-letter-skill/SKILL.md | grep 'ceus'"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "AskUserQuestion Enforcement" references/skill-skeleton.md` returns 1
    - `grep -c "Bilingual eligibility" references/skill-skeleton.md` returns 1
    - `grep "External MCP" .claude/skills/literature-skill/SKILL.md` matches line 16 with `- External MCP` (DEBT-02 verified)
    - `grep -A1 "required:" .claude/skills/cover-letter-skill/SKILL.md` shows `- references/journals/ceus.md` (DEBT-03 verified)
    - `git diff --name-only` shows ONLY `references/skill-conventions.md` and `references/skill-skeleton.md` as modified (no changes to Skill files)
    - Skeleton total is approximately 158-162 lines (155 original + 3-7 added lines)
  </acceptance_criteria>
  <done>
    skill-skeleton.md synced with two new notes (AskUserQuestion enforcement cross-reference, bilingual eligibility guidance). DEBT-02 verified: literature-skill uses "External MCP". DEBT-03 verified: cover-letter-skill lists ceus.md in required. No Skill files modified.
  </done>
</task>

</tasks>

<verification>
After both tasks complete, run the full verification suite:

```bash
bash -c '
echo "=== DEBT-01: Escape hatch Skill names ===" &&
grep "logic-skill\|visualization-skill\|literature-skill" references/skill-conventions.md &&
echo "" &&
echo "=== UXFIX-02: AskUserQuestion enforcement ===" &&
grep -c "AskUserQuestion" references/skill-conventions.md &&
echo "(expect >= 5)" &&
echo "" &&
echo "=== Bilingual eligibility ===" &&
grep "Bilingual Output Eligibility" references/skill-conventions.md &&
echo "" &&
echo "=== DEBT-02: literature-skill External MCP ===" &&
grep "External MCP" .claude/skills/literature-skill/SKILL.md &&
echo "" &&
echo "=== DEBT-03: cover-letter-skill CEUS in required ===" &&
grep -A1 "required:" .claude/skills/cover-letter-skill/SKILL.md &&
echo "" &&
echo "=== Skeleton sync ===" &&
grep "AskUserQuestion" references/skill-skeleton.md &&
grep "Bilingual" references/skill-skeleton.md &&
echo "" &&
echo "=== Files changed (should be exactly 2) ===" &&
git diff --name-only
'
```

Expected results:
- DEBT-01: Three Skill names appear in escape hatch line
- UXFIX-02: AskUserQuestion count >= 5
- Bilingual: "Bilingual Output Eligibility" heading present
- DEBT-02: "External MCP" confirmed in literature-skill
- DEBT-03: "ceus.md" in required field of cover-letter-skill
- Skeleton: Both sync additions present
- Only 2 files changed: `references/skill-conventions.md`, `references/skill-skeleton.md`
</verification>

<success_criteria>
1. `skill-conventions.md` escape hatch rule (DEBT-01) names `logic-skill`, `visualization-skill`, `literature-skill` as concrete examples
2. `skill-conventions.md` AskUserQuestion enforcement subsection (UXFIX-02) includes: per-Skill audit approach, direct-mode exemption, batch-mode note, good/bad comparison example, fallback cross-reference
3. `skill-conventions.md` bilingual output eligibility subsection lists 7 eligible Skills (default ON) and 4 exempt Skills in a table
4. `literature-skill/SKILL.md` confirmed to use "External MCP" (DEBT-02 — verify-only)
5. `cover-letter-skill/SKILL.md` confirmed to list CEUS in required (DEBT-03 — verify-only)
6. `skill-skeleton.md` synced with AskUserQuestion and bilingual cross-reference notes
7. No Skill files (.claude/skills/*/SKILL.md) were modified — only references/ files changed
</success_criteria>

<output>
After completion, create `.planning/phases/11-convention-tech-debt/11-01-SUMMARY.md`
</output>
